\newcommand{\statementtype}{proposition}
\newcommand{\statementoption}{}
\newcommand{\statementlabel}{prop:neural_process_objective_minimiser}
\newcommand{\statementcontent}{
    Assume that $\tI \sub I$ is dense.
    %Then $(\pi, \sigma)$ is a minimiser of $\L\ss{NP}$ over $\overline{\M}\ss{c}$ if and only if
    Then $(\pi, \sigma) \in \argmin_{\overline{\M}\ss{c}} \L\ss{NP}$ if and only if%
    \begin{equation}
        \pi_f|_{\tD} = \pi|_{\tD}
        \quad\text{and}\quad
        \sigma = \sigma_f.
    \end{equation}
}
